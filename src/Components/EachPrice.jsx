import React from 'react'
import styled from 'styled-components'

let Card = styled.div`
    display: flex;
`

let Logo = styled.img`
    width: 50px;
    height: 50px;
`

export default function EachPrice() {
    return (
        <Card>

        </Card>
    )
}
